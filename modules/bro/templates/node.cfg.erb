##Manged by Puppet##

[logger]
type=logger
host=localhost
#
[manager]
type=manager
host=localhost
#
[proxy-1]
type=proxy
host=localhost

## Insert via Hieradata in %nodename.yaml ##
<% @bro_workers.each do |worker| -%>
<%= worker %>
<% end -%>
